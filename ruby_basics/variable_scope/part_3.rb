a = 7

def my_value(b)
  a = b
end

my_value(a + 5)local variables inside the method definition are not visible outside the method definition
puts a

"This prints 7.
1. Because method definitions create their own 
scope outside the regular flow of execution"
local variables inside the method definition are not visible outside the method definitionlocal variables inside the method definition are not visible outside the method definition